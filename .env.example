# Database URLs
# To switch between development and production, change DATABASE_URL to use either DEV or PROD URL
DATABASE_URL=${NEXT_PUBLIC_DEV_URL} # For development
# DATABASE_URL=${NEXT_PUBLIC_PROD_URL} # For production

# Your Neon.tech database URLs
NEXT_PUBLIC_PROD_URL="your-production-neon-database-url"
NEXT_PUBLIC_DEV_URL="your-development-neon-database-url"

# NextAuth Configuration
# Generate a secure secret key using: openssl rand -base64 32
NEXTAUTH_SECRET="generate-a-secure-secret-key"
NEXTAUTH_URL="http://localhost:3000" # In production, change to your domain

# Admin User Configuration (for database seeding)
ADMIN_USERNAME="your-admin-username"
ADMIN_PASSWORD="your-secure-admin-password" 